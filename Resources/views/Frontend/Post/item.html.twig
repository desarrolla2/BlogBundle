<article class="post">
    <header>
        <h2><a href="{{ path('_view', { 'slug' : post.slug }) }}">{{ post }}</a></h2>            
    </header>
    {{ post.intro | raw }} 
    <small>
        Publicado el <time datetime="{{ post.createdAt | date('c') }}">{{ post.createdAt | localeDate('full') }}</time>.
    </small>
</article>