<ul class="tags">
    {% for tag in tags %}
        <li><a href="{{ url('_tag', { 'slug' : tag.slug }) }}">{{ tag }}</a></li>
    {% else %}
        <li>No tiene etiquetas</li>
    {% endfor %}
</ul>
